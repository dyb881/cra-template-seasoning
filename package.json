{
  "name": "cra-template-seasoning",
  "description": "基于cra-template-pwa和seasoning生成的业务模版",
  "version": "1.0.0",
  "author": "dyb881",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/dyb881/cra-template-seasoning"
  },
  "engines": {
    "node": ">=10.0.0"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "lerna:publish": "lerna publish",
    "changelog": "lerna-changelog",
    "format": "prettier --write \"**/*.{ts,js,json,md}\"",
    "format:check": "prettier --debug-check \"**/*.{ts,js,json,md}\"",
    "to:mobile": "rm -rf packages/cra-template-seasoning-mobile/template && cp -R packages/cra-template-seasoning/template packages/cra-template-seasoning-mobile/template && cp -R src/mobile/template packages/cra-template-seasoning-mobile",
    "to:pc": "rm -rf packages/cra-template-seasoning-pc/template && cp -R packages/cra-template-seasoning/template packages/cra-template-seasoning-pc/template && cp -R src/pc/template packages/cra-template-seasoning-pc",
    "to:all": "npm run to:mobile && npm run to:pc"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "*.{ts,js,tsx,html,json,md}": [
      "prettier --write"
    ]
  },
  "devDependencies": {
    "husky": "^4.2.5",
    "lerna": "^3.20.2",
    "lerna-changelog": "^1.0.1",
    "lint-staged": "^10.2.2",
    "prettier": "^2.0.5"
  },
  "publishConfig": {
    "registry": "https://registry.npmjs.org/"
  }
}
